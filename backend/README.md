# GameTracker API

```bash
cd backend
dotnet run
```

Varsayılan: `http://localhost:5118` — Swagger (geliştirme): `/swagger`.

**Yapılandırma ve deploy:** [docs/DEPLOY.md](../docs/DEPLOY.md) — IGDB için Twitch **Client ID / Secret**, detay tamamlayıcı için **RAWG** anahtarı.  
**Veritabanı (Somee, geçiş, LocalDB):** [docs/DATABASE.md](../docs/DATABASE.md)
