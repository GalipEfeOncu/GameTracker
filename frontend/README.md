# GameTracker — Frontend

React + Vite. API varsayılan olarak geliştirmede Vite proxy ile `http://localhost:5118` üzerinden çağrılır.

```bash
npm install
npm run dev
```

Kök depo özeti ve backend kurulumu için üst dizindeki `README.md` ve `backend/README.md` dosyalarına bakın.
