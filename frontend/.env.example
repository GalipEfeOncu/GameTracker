# Geliştirme: boş bırakın — istekler /api üzerinden Vite proxy’ye gider.
# Üretim build: bkz. .env.production.example (kopyalayıp .env.production yapın; o dosya git’e girmez)
VITE_API_BASE_URL=

# İsteğe bağlı: dotnet run adresi Vite varsayılanından (5118) farklıysa
# VITE_DEV_PROXY_TARGET=http://localhost:5118
